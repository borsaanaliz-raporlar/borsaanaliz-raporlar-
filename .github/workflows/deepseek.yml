name: AI Chat Assistant
on:
  workflow_dispatch:
  repository_dispatch:
    types: [chat_query]

jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install requests
        run: pip install requests
      
      - name: Run AI Assistant
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: python .github/openrouter.py
      
      - name: Show response
        run: |
          echo "📊 İŞLEM SONUÇLARI:"
          if [ -f answer.txt ]; then
            echo "✅ Yanıt başarıyla oluşturuldu!"
            echo "--- YANIT BAŞLANGICI ---"
            head -c 500 answer.txt
            echo ""
            echo "--- YANIT SONU ---"
          else
            echo "❌ Yanıt dosyası oluşturulamadı"
          fi
