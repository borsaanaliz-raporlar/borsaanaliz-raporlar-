name: 🤖 Excel-Okuyan AI Yanıt

on:
  issues:
    types: [opened]
  workflow_dispatch:

jobs:
  excel-ai:
    
    runs-on: ubuntu-latest
    
    permissions:
      issues: write
      contents: read
    
    steps:
    - name: 📥 Repository'yi çek
      uses: actions/checkout@v3
      
    - name: 🐍 Python kur
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
      
    - name: 📦 Kütüphaneleri kur
      run: |
        pip install pandas openpyxl requests
        echo "✅ Kütüphaneler kuruldu"
      
    - name: 🔍 Excel dosyalarını kontrol et
      run: |
        echo "📂 Mevcut dosyalar:"
        ls -la *.xls* || echo "Excel dosyası yok"
      
    - name: 🤖 Excel AI analizini çalıştır
      env:
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      run: python .github/excel_ai_analyzer.py "${{ github.event.issue.body }}"
      
    - name: 📝 Yanıt dosyasını kontrol et (YENİ EKLENDİ)
      id: check-response
      run: |
        if [ -f "ai_response.txt" ]; then
          echo "✅ Yanıt dosyası bulundu."
          echo "has_response=true" >> $GITHUB_OUTPUT
        else
          echo "❌ Hata: ai_response.txt bulunamadı!"
          echo "has_response=false" >> $GITHUB_OUTPUT
          # Acil durum yanıtı oluştur
          echo "Üzgünüm, Excel AI yanıtı şu anda oluşturulamadı. Lütfen daha sonra tekrar deneyin veya soruyu manuel kontrol edin." > ai_response.txt
        fi
      
    - name: 💬 Yanıtı Issue'a ekle
      uses: actions/github-script@v6
      env:
        AI_RESPONSE: ${{ steps.check-response.outputs.has_response }}
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          const fs = require('fs');
          let response = "AI yanıtı oluşturulamadı.";
          
          try {
            response = fs.readFileSync('ai_response.txt', 'utf8');
          } catch (e) {
            response = `⚠️ Teknik bir hata oluştu: ${e.message}. Lütfen daha sonra tekrar deneyin.`;
          }
          
          // Yanıtı Issue'a yorum olarak ekle
          await github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: `## 📈 EXCEL AI ANALİZ RAPORU\n\n${response}\n\n---\n⚡ **Sistem:** Otomatik Excel Okuyucu AI\n📅 **Analiz Tarihi:** ${new Date().toLocaleString('tr-TR')}\n🤖 **AI Model:** Groq Llama 3.1`
          });
          
          // İsteğe bağlı: Issue'ya otomatik olarak bir label ekleyin
          try {
            await github.rest.issues.addLabels({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['excel-ai-cevaplandı']
            });
          } catch (error) {
            console.log("Label eklenemedi, önemli değil.");
          }
          
          console.log("✅ Excel AI yanıtı Issue'a eklendi!");
